<%= task_header(@task_connection.source) %>

<h2>Mark As Duplicate</h2>

<p>
  This task is similar to another task that is already being worked on.
  Pick another task that should override this one:
</p>

<%= render 'form', task_connection: @task_connection %>

<p><%= link_to 'Back', :back %></p>
