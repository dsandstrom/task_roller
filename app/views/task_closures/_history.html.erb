<%
  value = "closed (##{task_closure.task_id}): "\
          "#{format_date task_closure.created_at}"
%>

<%= content_tag :dt, task_closure.user&.name_or_email,
                class: 'task-closure-user' %>
<%= content_tag :dd, value,
                class: "task-closure-#{task_closure.id}" %>
