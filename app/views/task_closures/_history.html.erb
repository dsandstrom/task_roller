<%
  value = "closed (##{history.task_id}): "\
          "#{format_date history.created_at}"
%>

<%= content_tag :dt, history.user&.name_or_email,
                class: 'task-closure-user' %>
<%= content_tag :dd, value,
                class: 'task-closure',
                id: "task-closure-#{history.id}" %>
