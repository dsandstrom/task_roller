<% progress = assignee.task_progress(task) %>

<dt id="assignee-<%= assignee.id %>" class="assignee">
  <%= content_tag :strong, assignee.name_or_email,
                  class: 'assignee-name' %>
</dt>

<% if progress.present? %>
  <dd id="assignee-progressions-<%= assignee.id %>"
      class="assignee-progressions">
    made progress:
    <%= progress %>
  </dd>
<% end %>
