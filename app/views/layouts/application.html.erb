<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? content_for(:title) : 'TaskRoller' %></title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Issue tracking app with the added ability to assign Tasks to address your projects' bugs and feature requests.">

    <%= stylesheet_link_tag 'application', media: 'all',
                            'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'accordions', 'check_boxes',
                            'dropdowns', 'flash_messages', 'forms',
                            'radio_buttons', 'search_forms',
                            'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="main-container">
      <%= render 'layouts/header' %>

      <main class="main-content">
        <div class="page">
          <div class="columns">
            <div class="first-column">
              <%= yield %>
            </div>
            <% if content_for?(:sidebar) %>
              <div class="second-column">
                <%= yield :sidebar %>
              </div>
            <% end %>
          </div>
        </div>
      </main>

      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
