<%= turbo_stream.replace "turbo_issue_tasks_#{@issue.id}",
                         collection: @tasks,
                         partial: 'issues/task',
                         locals: { issue: @issue } %>
