var showCommentId = 'comment-' + <%= @task_comment.id %>;
var showComment = document.getElementById(showCommentId);

if (showComment) {
  showComment.outerHTML = "<%= j render('shared/comment', object: @task, comment: @task_comment) %>";
}
