<%
  value = "marked as duplicate (##{history.target_id}): "\
          "#{format_date history.created_at}"
%>

<%= content_tag :dt, history.user&.name_or_email,
                class: 'issue-connection-user' %>
<%= content_tag :dd, value,
                class: "issue-connection-#{history.id}" %>
