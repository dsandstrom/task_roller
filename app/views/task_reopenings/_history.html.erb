<%
  value = "reopened (##{history.task_id}): "\
          "#{format_date history.created_at}"
%>

<%= content_tag :dt, history.user&.name_or_email,
                class: 'task-reopening-user' %>
<%= content_tag :dd, value,
                class: "task-reopening",
                id: "task-reopening-#{history.id}" %>
