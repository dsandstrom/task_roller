<%= form_with(model: project_issues_subscription,
              url: project_issues_subscription_path(project, project_issues_subscription),
              method: :delete,
              html: { name: 'project_issue_subscription_destroy_form',
                      class: 'issue-subscription-form' },
              data: { turbo_method: 'delete' }) do |form| %>
  <%= button_tag class: 'button button-success button-unsubscribe button-icon',
                 title: 'Unsubscribe',
                 data: { disable_with: '...', turbo_submits_with: '...' } do %>
    <%= icon('close') %>
  <% end %>
<% end %>
