<%= category_index_header %>

<header class="categories-header">
</header>

<div class="categories">
  <% if @categories.any? %>
    <%= render @categories %>
  <% else %>
    <p class="no-category">none</p>
  <% end %>
</div>
