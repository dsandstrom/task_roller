<%= project_header(@project) %>

<h2>Report Issue</h2>

<%= render 'form', issue: @issue, issue_types: @issue_types,
                   url: category_project_issues_path(@category, @project),
                   user_options: @user_options %>

<%= link_to 'Back', :back %>
