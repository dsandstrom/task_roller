<%
  value = "closed (##{issue_closure.issue_id}): "\
          "#{format_date issue_closure.created_at}"
%>

<%= content_tag :dt, issue_closure.user&.name_or_email,
                class: 'issue-closure-user' %>
<%= content_tag :dd, value,
                class: "issue-closure-#{issue_closure.id}" %>
