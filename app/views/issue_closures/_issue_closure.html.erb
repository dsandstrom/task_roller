<% if can?(:destroy, issue_closure) %>
  <%
    confirm = "Are you sure you want to remove the closure for "\
              issue_closure.issue.heading
  %>

  <p>
    <%= link_to 'remove closure',
                 issue_closure_path(issue_closure.issue, issue_closure),
                 method: :delete, class: 'destroy-link',
                 data: { confirm: confirm } %>
  </p>
<% end %>
